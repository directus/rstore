import type { TodoCollection, UserCollection } from './schema'

export function defineTodoRelations(_todoCollection: typeof TodoCollection, _userCollection: typeof UserCollection) {
  return undefined
}
